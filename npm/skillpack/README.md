# @nikhilp0/skillpack

Installs the Skillpack CLI binary and exposes `sp` on your PATH.

## Install

```bash
npm install -g @nikhilp0/skillpack
```

## Notes

- Downloads the release binary for your platform from GitHub.
- Supported: macOS (x64/arm64), Linux x64, Windows x64.
